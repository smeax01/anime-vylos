---
import Layout from "../../layouts/Layout.astro";
import { getAnimeById } from "../../lib/anilist";
import { ArrowLeft, Play, Layers } from "lucide-astro";

export const prerender = false;

const { id } = Astro.params;
const animeId = Number(id);

let anime = null;
try {
  anime = await getAnimeById(animeId);
} catch (e) {
  console.error(e);
}

if (!anime) return Astro.redirect("/404");

const title = anime.title.romaji || anime.title.english;
// Filter relations to show other seasons and sequels
const relations =
  anime.relations?.edges?.filter((e) => e.node.type === "ANIME") || [];
---

<Layout title={title}>
  <div class="player-page">
    <div class="top-nav">
      <a href="/" class="back-btn"><ArrowLeft size={24} /></a>
      <span class="nav-title">{title}</span>
    </div>

    <div class="video-container" id="video-wrapper">
      <div
        id="player-placeholder"
        class="placeholder"
        style={`background-image: url(${anime.bannerImage || anime.coverImage.extraLarge})`}
      >
        <div class="loading-overlay hidden" id="loading-overlay">
          <div class="spinner"></div>
          <span>Recherche des sources...</span>
        </div>
        <button
          id="start-btn"
          class="play-btn-large"
          data-id={anime.id}
          data-title={title}
        >
          <Play fill="currentColor" size={32} />
          <span>Lancer la lecture</span>
        </button>
      </div>
      <div id="fallback-ui" class="hidden fallback-ui">
        <h3 style="color:#ef4444;">
          Impossible de lire la vidéo directement 😕
        </h3>
        <p>
          Les serveurs de streaming gratuits sont instables. Utilisez un lien
          externe :
        </p>
        <div class="fallback-actions">
          <a
            href={`https://anitaku.to/search.html?keyword=${encodeURIComponent(title)}`}
            target="_blank"
            class="btn-alt">GogoAnime</a
          >
          <a
            href={`https://hianime.to/search?keyword=${encodeURIComponent(title)}`}
            target="_blank"
            class="btn-alt">HiAnime</a
          >
        </div>
      </div>
      <video id="player" controls crossorigin playsinline></video>
    </div>

    <div class="container info-section">
      <div class="main-info">
        <h1 class="anime-title">{title}</h1>
        <div class="meta-row">
          <span class="meta-tag rating"
            >★ {anime.averageScore ? anime.averageScore / 10 : "?"}</span
          >
          <span class="meta-tag status">{anime.status}</span>
          <span class="meta-tag">{anime.episodes || "?"} Eps</span>
          <span class="meta-tag">{anime.format}</span>
        </div>
        <p class="description" set:html={anime.description} />
      </div>

      <!-- Genres -->
      <div class="genres">
        {anime.genres?.map((g) => <span class="genre-pill">{g}</span>)}
      </div>

      <!-- Saisons / Relations -->
      {
        relations.length > 0 && (
          <div class="relations-section">
            <h3 class="section-header">
              <Layers size={18} /> Saisons & Suites
            </h3>
            <div class="relations-grid">
              {relations.map((rel) => (
                <a href={`/anime/${rel.node.id}`} class="rel-card">
                  <div class="rel-img">
                    <img
                      src={rel.node.coverImage.medium}
                      alt={rel.node.title.romaji}
                      loading="lazy"
                    />
                    <div class="rel-badge">{rel.relationType}</div>
                  </div>
                  <div class="rel-title">{rel.node.title.romaji}</div>
                </a>
              ))}
            </div>
          </div>
        )
      }

      <!-- Liste des épisodes -->
      <h3 class="section-header">Épisodes</h3>
      <div id="episodes-grid" class="episodes-grid">
        <div class="loading-eps">Chargement des épisodes...</div>
      </div>
    </div>
  </div>
</Layout>

<script>
  import Hls from "hls.js";
  import Plyr from "plyr";
  import "plyr/dist/plyr.css";

  const video = document.getElementById("player") as HTMLVideoElement;
  const startBtn = document.getElementById("start-btn");
  const loadingOverlay = document.getElementById("loading-overlay");
  const placeholder = document.getElementById("player-placeholder");
  const fallbackUI = document.getElementById("fallback-ui");
  const episodesGrid = document.getElementById("episodes-grid");
  let player: Plyr;

  const animeId = startBtn?.dataset.id;
  const animeTitle = startBtn?.dataset.title;

  // List of public API instances to try
  const API_INSTANCES = [
    "https://api-consumet.vercel.app",
    "https://consumet-api-nine.vercel.app",
    "https://api.consumet.org",
  ];

  async function fetchWithFallback(path: string) {
    for (const api of API_INSTANCES) {
      try {
        const res = await fetch(`${api}${path}`);
        if (res.ok) return await res.json();
      } catch (e) {
        console.warn(`API ${api} failed, trying next...`);
      }
    }
    throw new Error("All API instances failed");
  }

  async function init() {
    if (!animeId) return;
    loadingOverlay?.classList.remove("hidden");

    try {
      // Search for episodes
      let data;
      try {
        data = await fetchWithFallback(`/meta/anilist/info/${animeId}`);
      } catch (e) {
        // Fallback to title search if ID mapping fails
        const search = await fetchWithFallback(
          `/meta/anilist/${encodeURIComponent(animeTitle!)}`
        );
        if (search.results && search.results.length > 0) {
          data = await fetchWithFallback(
            `/meta/anilist/info/${search.results[0].id}`
          );
        }
      }

      if (data && data.episodes && data.episodes.length > 0) {
        renderEpisodes(data.episodes);
      } else {
        throw new Error("No episodes");
      }
    } catch (e) {
      console.error(e);
      episodesGrid!.innerHTML =
        '<div class="error-eps">Flux temporairement indisponible. Utilisez les liens externes.</div>';
    } finally {
      loadingOverlay?.classList.add("hidden");
    }
  }

  function renderEpisodes(episodes: any[]) {
    episodesGrid!.innerHTML = "";
    episodes.forEach((ep: any) => {
      const btn = document.createElement("button");
      btn.className = "episode-card";
      btn.onclick = () => loadEpisode(ep.id);
      btn.innerHTML = `
             <div class="ep-thumb">
                ${ep.image ? `<img src="${ep.image}" />` : `<div class="no-img">${ep.number}</div>`}
             </div>
             <div class="ep-info">
               <span class="ep-num">Épisode ${ep.number}</span>
               <span class="ep-title">${ep.title || `Épisode ${ep.number}`}</span>
             </div>
          `;
      episodesGrid!.appendChild(btn);
    });
    if (startBtn) startBtn.onclick = () => loadEpisode(episodes[0].id);
  }

  async function loadEpisode(episodeId: string) {
    placeholder!.style.display = "flex";
    loadingOverlay?.classList.remove("hidden");
    fallbackUI?.classList.add("hidden");
    video.style.display = "none";

    try {
      const data = await fetchWithFallback(`/meta/anilist/watch/${episodeId}`);
      const source =
        data.sources?.find(
          (s: any) => s.quality === "1080p" || s.quality === "720p"
        ) || data.sources?.[0];

      if (source?.url) {
        setupPlayer(source.url, source.isM3U8 || source.url.includes(".m3u8"));
        placeholder!.style.display = "none";
        video.style.display = "block";
      } else {
        throw new Error("No stream");
      }
    } catch (e) {
      console.error(e);
      showFallback();
    } finally {
      loadingOverlay?.classList.add("hidden");
    }
  }

  function showFallback() {
    placeholder!.style.display = "none";
    video.style.display = "none";
    fallbackUI?.classList.remove("hidden");
  }

  function setupPlayer(sourceUrl: string, isHls: boolean) {
    if (player) player.destroy();
    if (isHls && Hls.isSupported()) {
      const hls = new Hls();
      hls.loadSource(sourceUrl);
      hls.attachMedia(video);
      hls.on(Hls.Events.MANIFEST_PARSED, () => video.play().catch(() => {}));
    } else {
      video.src = sourceUrl;
      video.play().catch(() => {});
    }
    player = new Plyr(video, {
      controls: [
        "play-large",
        "play",
        "progress",
        "current-time",
        "mute",
        "volume",
        "fullscreen",
        "settings",
      ],
      settings: ["quality"],
    });
  }

  init();
</script>

<style>
  .hidden {
    display: none !important;
  }
  .player-page {
    padding-bottom: 120px;
  }

  .top-nav {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    z-index: 50;
    background: linear-gradient(
      180deg,
      rgba(2, 6, 23, 0.9) 0%,
      transparent 100%
    );
  }

  .back-btn {
    color: white;
    background: rgba(255, 255, 255, 0.1);
    padding: 0.5rem;
    border-radius: var(--radius-md);
    display: flex;
    backdrop-filter: blur(4px);
  }

  .video-container {
    width: 100%;
    aspect-ratio: 16/9;
    background: #000;
    position: relative;
    max-height: 500px;
  }
  .placeholder {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .placeholder::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(2px);
  }

  .spinner {
    width: 30px;
    height: 30px;
    border: 3px solid rgba(255, 255, 255, 0.3);
    border-top-color: var(--color-primary);
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
  }
  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }

  .fallback-ui {
    position: absolute;
    inset: 0;
    background: rgba(15, 23, 42, 0.98);
    z-index: 30;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    padding: 2rem;
    text-align: center;
  }
  .btn-alt {
    background: var(--color-primary);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: var(--radius-md);
    text-decoration: none;
    font-weight: 700;
    transition: transform 0.2s;
  }
  .btn-alt:hover {
    transform: scale(1.1);
  }

  .info-section {
    padding-top: 2rem;
  }
  .anime-title {
    font-size: 1.8rem;
    font-weight: 900;
    margin-bottom: 0.5rem;
  }
  .meta-row {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
    font-size: 0.85rem;
  }
  .meta-tag {
    background: #1e293b;
    padding: 0.25rem 0.75rem;
    border-radius: var(--radius-sm);
    color: #94a3b8;
  }
  .rating {
    color: #fbbf24;
    font-weight: bold;
  }

  .description {
    color: #cbd5e1;
    line-height: 1.7;
    font-size: 0.95rem;
    margin-bottom: 2rem;
    max-height: 200px;
    overflow-y: auto;
    padding-right: 0.5rem;
  }

  .genres {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    margin-bottom: 3rem;
  }
  .genre-pill {
    color: var(--color-accent);
    font-size: 0.75rem;
    font-weight: 600;
    background: rgba(56, 189, 248, 0.1);
    padding: 4px 12px;
    border-radius: 20px;
    border: 1px solid rgba(56, 189, 248, 0.2);
  }

  /* Saisons */
  .relations-section {
    margin-bottom: 3rem;
  }
  .relations-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
    gap: 1rem;
  }
  .rel-card {
    text-decoration: none;
    transition: transform 0.2s;
  }
  .rel-card:hover {
    transform: translateY(-5px);
  }
  .rel-img {
    position: relative;
    aspect-ratio: 2/3;
    border-radius: var(--radius-md);
    overflow: hidden;
    margin-bottom: 0.5rem;
  }
  .rel-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .rel-badge {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 4px;
    background: rgba(0, 0, 0, 0.8);
    color: var(--color-primary);
    font-size: 0.6rem;
    font-weight: 800;
    text-align: center;
    text-transform: uppercase;
  }
  .rel-title {
    font-size: 0.8rem;
    font-weight: 600;
    color: white;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    line-height: 1.3;
  }

  .section-header {
    font-size: 1.3rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: white;
  }

  .episodes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 1rem;
  }
  :global(.episode-card) {
    background: #1e293b;
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-md);
    overflow: hidden;
    cursor: pointer;
    text-align: left;
    padding: 0;
    transition: border-color 0.2s;
  }
  :global(.episode-card:hover) {
    border-color: var(--color-primary);
  }
  :global(.ep-thumb) {
    aspect-ratio: 16/9;
    background: #0f172a;
    position: relative;
  }
  :global(.ep-thumb img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.8;
  }
  :global(.ep-info) {
    padding: 0.75rem;
  }
  :global(.ep-num) {
    font-size: 0.7rem;
    color: var(--color-primary);
    font-weight: 700;
    text-transform: uppercase;
  }
  :global(.ep-title) {
    font-size: 0.85rem;
    font-weight: 600;
    color: white;
    display: block;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    margin-top: 2px;
  }

  .error-eps {
    padding: 2rem;
    color: #ef4444;
    font-weight: 600;
    text-align: center;
    grid-column: 1/-1;
    background: rgba(239, 68, 68, 0.05);
    border-radius: var(--radius-md);
  }
</style>
