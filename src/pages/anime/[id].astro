---
import Layout from "../../layouts/Layout.astro";
import { getAnimeById } from "../../lib/anilist";
import { ArrowLeft, Play, Layers } from "lucide-astro";

export const prerender = false;

const { id } = Astro.params;
const animeId = Number(id);

let anime = null;
try {
  anime = await getAnimeById(animeId);
} catch (e) {}

if (!anime) return Astro.redirect("/404");

const title = anime.title.romaji || anime.title.english;
const relations =
  anime.relations?.edges?.filter((e) => e.node.type === "ANIME") || [];
---

<Layout title={title}>
  <div class="player-page">
    <div class="top-nav">
      <a href="/" class="back-btn"><ArrowLeft size={24} /></a>
      <span class="nav-title">{title}</span>
    </div>

    <div class="video-container" id="video-wrapper">
      <div
        id="player-placeholder"
        class="placeholder"
        style={`background-image: url(${anime.bannerImage || anime.coverImage.extraLarge})`}
      >
        <div class="loading-overlay hidden" id="loading-overlay">
          <div class="spinner"></div>
          <span id="loading-text">Recherche des épisodes...</span>
        </div>
        <button
          id="start-btn"
          class="play-btn-large"
          data-id={anime.id}
          data-title={title}
          data-max-ep={anime.episodes}
        >
          <Play fill="currentColor" size={32} />
          <span>Lancer la lecture</span>
        </button>
      </div>
      <div id="fallback-ui" class="hidden fallback-ui">
        <h3>Source interne introuvable 😕</h3>
        <p>
          Cet anime (ou cet épisode) n'est pas disponible sur nos serveurs
          directs.
        </p>
        <div class="fallback-actions">
          <a id="fb-gogo" href="#" target="_blank" class="btn-alt"
            >Regarder sur GogoAnime</a
          >
          <a id="fb-hi" href="#" target="_blank" class="btn-alt"
            >Regarder sur HiAnime</a
          >
        </div>
      </div>
      <video id="player" controls crossorigin playsinline></video>
    </div>

    <div class="container info-section">
      <h1 class="anime-title">{title}</h1>
      <div class="meta-row">
        <span class="meta-tag rating"
          >★ {anime.averageScore ? anime.averageScore / 10 : "?"}</span
        >
        <span class="meta-tag">{anime.status}</span>
        <span class="meta-tag">{anime.episodes || "?"} Eps</span>
        <span class="meta-tag">{anime.format}</span>
      </div>
      <p class="description" set:html={anime.description} />

      {
        relations.length > 0 && (
          <div class="relations-section">
            <h3 class="section-header">
              <Layers size={18} /> Saisons & Suites
            </h3>
            <div class="relations-scroll">
              {relations.map((rel) => (
                <a href={`/anime/${rel.node.id}`} class="rel-card">
                  <img
                    src={rel.node.coverImage.medium}
                    alt={rel.node.title.romaji}
                    loading="lazy"
                  />
                  <div class="rel-info">
                    <span class="rel-type">{rel.relationType}</span>
                    <span class="rel-title">{rel.node.title.romaji}</span>
                  </div>
                </a>
              ))}
            </div>
          </div>
        )
      }

      <h3 class="section-header">Épisodes</h3>
      <div id="episodes-grid" class="episodes-grid">
        <div class="loading-eps">Chargement des données...</div>
      </div>
    </div>
  </div>
</Layout>

<script>
  import Hls from "hls.js";
  import Plyr from "plyr";
  import "plyr/dist/plyr.css";

  const video = document.getElementById("player") as HTMLVideoElement;
  const startBtn = document.getElementById("start-btn");
  const loadingOverlay = document.getElementById("loading-overlay");
  const loadingText = document.getElementById("loading-text");
  const placeholder = document.getElementById("player-placeholder");
  const fallbackUI = document.getElementById("fallback-ui");
  const episodesGrid = document.getElementById("episodes-grid");

  const fbGogo = document.getElementById("fb-gogo") as HTMLAnchorElement;
  const fbHi = document.getElementById("fb-hi") as HTMLAnchorElement;

  let player: Plyr;
  const animeId = startBtn?.dataset.id;
  const animeTitle = startBtn?.dataset.title;
  const maxEpCount = Number(startBtn?.dataset.maxEp || 0);

  const API_BASE = "https://api-consumet.vercel.app";

  // State to store which provider we are using
  let currentEpisodes: any[] = [];

  async function fetchEpisodes() {
    if (!animeId) return;
    loadingOverlay?.classList.remove("hidden");

    try {
      // 1. Try Meta AniList (Most accurate)
      loadingText!.innerText = "Sync avec AniList...";
      const res = await fetch(`${API_BASE}/meta/anilist/info/${animeId}`);
      const data = await res.json();

      if (data.episodes && data.episodes.length > 0) {
        currentEpisodes = data.episodes;
      } else {
        // 2. Try Title Search with Provider GogoAnime
        loadingText!.innerText = "Recherche via GogoAnime...";
        const searchRes = await fetch(
          `${API_BASE}/anime/gogoanime/${encodeURIComponent(animeTitle!)}`
        );
        const searchData = await searchRes.json();
        if (searchData.results && searchData.results.length > 0) {
          const infoRes = await fetch(
            `${API_BASE}/anime/gogoanime/info/${searchData.results[0].id}`
          );
          const infoData = await infoRes.json();
          currentEpisodes = infoData.episodes;
        }
      }

      if (currentEpisodes.length > 0) {
        renderEpisodes(currentEpisodes);
      } else {
        throw new Error("No episodes found anywhere");
      }
    } catch (e) {
      console.warn(
        "Internal search failed, providing manual list for external playback."
      );
      renderManualEpisodes();
    } finally {
      loadingOverlay?.classList.add("hidden");
    }
  }

  function renderManualEpisodes() {
    const list = [];
    const count = maxEpCount > 0 ? maxEpCount : 12;
    for (let i = 1; i <= count; i++) {
      list.push({ id: `manual-${i}`, number: i, title: `Épisode ${i}` });
    }
    renderEpisodes(list, true);
  }

  function renderEpisodes(episodes: any[], isManual = false) {
    episodesGrid!.innerHTML = "";
    episodes.forEach((ep) => {
      const btn = document.createElement("button");
      btn.className = "episode-card";
      btn.onclick = () =>
        isManual ? showFallback(ep.number) : loadEpisode(ep.id, ep.number);
      btn.innerHTML = `
        <div class="ep-thumb"><div class="no-img">${ep.number}</div></div>
        <div class="ep-info">
          <span class="ep-num">Épisode ${ep.number}</span>
          <span class="ep-title">${ep.title || `Épisode ${ep.number}`}</span>
        </div>
      `;
      episodesGrid!.appendChild(btn);
    });

    if (startBtn) {
      startBtn.onclick = () =>
        isManual ? showFallback(1) : loadEpisode(episodes[0].id, 1);
    }
  }

  async function loadEpisode(id: string, num: number) {
    loadingOverlay?.classList.remove("hidden");
    loadingText!.innerText = `Chargement Épisode ${num}...`;
    fallbackUI?.classList.add("hidden");
    video.style.display = "none";
    placeholder!.style.display = "flex";

    try {
      const res = await fetch(`${API_BASE}/meta/anilist/watch/${id}`);
      const data = await res.json();
      const source =
        data.sources?.find(
          (s: any) => s.quality === "default" || s.quality === "1080p"
        ) || data.sources?.[0];

      if (source?.url) {
        setupPlayer(source.url, source.isM3U8 || source.url.includes(".m3u8"));
        placeholder!.style.display = "none";
        video.style.display = "block";
      } else {
        throw new Error();
      }
    } catch (e) {
      showFallback(num);
    } finally {
      loadingOverlay?.classList.add("hidden");
    }
  }

  function showFallback(num: number) {
    placeholder!.style.display = "none";
    video.style.display = "none";
    fallbackUI?.classList.remove("hidden");
    fbGogo.href = `https://anitaku.to/search.html?keyword=${encodeURIComponent(animeTitle!)}`;
    fbHi.href = `https://hianime.to/search?keyword=${encodeURIComponent(animeTitle!)}`;
  }

  function setupPlayer(url: string, isHls: boolean) {
    if (player) player.destroy();
    if (isHls && Hls.isSupported()) {
      const hls = new Hls();
      hls.loadSource(url);
      hls.attachMedia(video);
    } else {
      video.src = url;
    }
    player = new Plyr(video, {
      controls: [
        "play-large",
        "play",
        "progress",
        "current-time",
        "mute",
        "volume",
        "fullscreen",
        "settings",
      ],
    });
    video.play().catch(() => {});
  }

  fetchEpisodes();
</script>

<style>
  .hidden {
    display: none !important;
  }
  .player-page {
    padding-bottom: 100px;
  }
  .top-nav {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    z-index: 50;
    background: linear-gradient(
      180deg,
      rgba(2, 6, 23, 0.9) 0%,
      transparent 100%
    );
  }
  .back-btn {
    color: white;
    background: rgba(255, 255, 255, 0.1);
    padding: 0.5rem;
    border-radius: var(--radius-md);
    display: flex;
    backdrop-filter: blur(4px);
  }
  .nav-title {
    font-weight: 600;
    color: white;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .video-container {
    width: 100%;
    aspect-ratio: 16/9;
    background: #000;
    position: relative;
    max-height: 500px;
  }
  .placeholder {
    position: absolute;
    inset: 0;
    background-size: cover;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #0f172a;
  }
  .placeholder::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.65);
    backdrop-filter: blur(2px);
  }

  .loading-overlay {
    position: absolute;
    z-index: 20;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    color: white;
  }
  .spinner {
    width: 30px;
    height: 30px;
    border: 3px solid rgba(255, 255, 255, 0.2);
    border-top-color: var(--color-primary);
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
  }
  @keyframes spin {
    to {
      transform: rotate(360deg);
    }
  }

  .fallback-ui {
    position: absolute;
    inset: 0;
    background: rgba(15, 23, 42, 0.98);
    z-index: 30;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    padding: 2rem;
    text-align: center;
  }
  .btn-alt {
    background: var(--color-primary);
    color: white;
    padding: 0.8rem 1.5rem;
    border-radius: var(--radius-md);
    text-decoration: none;
    font-weight: 700;
    font-size: 0.9rem;
    transition: transform 0.2s;
  }
  .btn-alt:hover {
    transform: scale(1.05);
  }

  .info-section {
    padding-top: 2rem;
  }
  .anime-title {
    font-size: 1.8rem;
    font-weight: 900;
    margin-bottom: 0.5rem;
  }
  .meta-row {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
    font-size: 0.85rem;
    color: #94a3b8;
  }
  .meta-tag {
    background: #1e293b;
    padding: 0.25rem 0.75rem;
    border-radius: 4px;
  }
  .rating {
    color: #fbbf24;
    font-weight: bold;
  }
  .description {
    color: #cbd5e1;
    line-height: 1.7;
    font-size: 0.95rem;
    margin-bottom: 2.5rem;
    max-height: 150px;
    overflow-y: auto;
  }

  .relations-section {
    margin-bottom: 3rem;
  }
  .relations-scroll {
    display: flex;
    gap: 1rem;
    overflow-x: auto;
    padding-bottom: 1rem;
    scrollbar-width: none;
  }
  .rel-card {
    flex: 0 0 120px;
    text-decoration: none;
    background: #1e293b;
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }
  .rel-card img {
    width: 100%;
    height: 160px;
    object-fit: cover;
  }
  .rel-info {
    padding: 0.5rem;
  }
  .rel-type {
    font-size: 0.6rem;
    color: var(--color-primary);
    font-weight: 800;
    text-transform: uppercase;
    display: block;
    margin-bottom: 2px;
  }
  .rel-title {
    font-size: 0.75rem;
    color: white;
    font-weight: 600;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    display: block;
  }

  .section-header {
    font-size: 1.3rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
    border-left: 4px solid var(--color-primary);
    padding-left: 0.75rem;
    color: white;
  }
  .episodes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: 1rem;
  }
  :global(.episode-card) {
    background: #1e293b;
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 10px;
    overflow: hidden;
    padding: 0;
    cursor: pointer;
    text-align: left;
    transition: all 0.2s;
  }
  :global(.episode-card:hover) {
    border-color: var(--color-primary);
    transform: translateY(-3px);
  }
  :global(.no-img) {
    aspect-ratio: 16/9;
    background: #0f172a;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    font-weight: 900;
    color: #334155;
  }
  :global(.ep-info) {
    padding: 0.75rem;
  }
  :global(.ep-num) {
    font-size: 0.7rem;
    color: var(--color-primary);
    font-weight: 800;
    text-transform: uppercase;
  }
  :global(.ep-title) {
    font-size: 0.85rem;
    font-weight: 600;
    color: white;
    display: block;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .loading-eps {
    padding: 3rem;
    text-align: center;
    color: var(--color-text-muted);
    width: 100%;
    grid-column: 1/-1;
  }

  .play-btn-large {
    position: relative;
    z-index: 10;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.6rem;
    background: var(--color-primary);
    border: none;
    color: white;
    padding: 1.2rem 2.5rem;
    border-radius: var(--radius-md);
    font-weight: 700;
    cursor: pointer;
    transition: all 0.2s;
    box-shadow: 0 10px 30px rgba(59, 130, 246, 0.4);
  }
  .play-btn-large:hover {
    transform: scale(1.05);
    filter: brightness(1.1);
  }
</style>
